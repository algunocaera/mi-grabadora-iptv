name: Monitor IPTVChannels
on:
  schedule:
    # Cambiamos al minuto 27 para evitar las horas punta de tráfico en GitHub
    - cron: '27 * * * *' 
  workflow_dispatch: # Permite ejecutarlo manualmente

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Instalar dependencias
        run: pip install requests
        
      - name: Ejecutar script
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: python check_changes.py
        
      - name: Guardar estado si hubo cambios
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add last_sha.txt
          git commit -m "Actualizar memoria de cambios" || exit 0
          git push
